# ===========================================
# COSMO IDE Studio (Standalone) Configuration
# ===========================================

# --------------------------------------------
# API Keys (At least ONE required)
# --------------------------------------------
# Get keys from:
# - OpenAI: https://platform.openai.com/api-keys
# - Anthropic: https://console.anthropic.com/settings/keys
# - xAI (Grok): https://console.x.ai/
# - Ollama Cloud: https://ollama.com/settings/api-keys

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
OLLAMA_CLOUD_API_KEY=

# --------------------------------------------
# Encryption Key (REQUIRED - auto-generated on first run)
# --------------------------------------------
# Used to encrypt OAuth tokens in the database
# The wizard will generate this automatically
ENCRYPTION_KEY=

# --------------------------------------------
# IDE Studio Ports (Independent from Unified)
# --------------------------------------------
PORT=4405                    # HTTP port for full IDE
HTTPS_PORT=4406              # HTTPS port for full IDE
BROWSER_PORT=4398            # Port for Brain Browser

# --------------------------------------------
# Security Profile (local | internet)
# --------------------------------------------
SECURITY_PROFILE=local

# Internet profile requires all values below:
# - reverse proxy must inject `x-evobrew-proxy-secret` and an auth user header
# - workspace root becomes the hard file-system boundary
EVOBREW_PROXY_SHARED_SECRET=
WORKSPACE_ROOT=
INTERNET_ENABLE_MUTATIONS=false
INTERNET_ENABLE_GATEWAY_PROXY=false
INTERNET_ENABLE_TERMINAL=false
ONLYOFFICE_CALLBACK_ALLOWLIST=
COLLABORA_SECRET=

# --------------------------------------------
# Terminal (PTY) Configuration
# --------------------------------------------
# Set TERMINAL_ENABLED=false to disable terminal feature entirely.
TERMINAL_ENABLED=true
# Max concurrent terminal sessions per client id.
TERMINAL_MAX_SESSIONS_PER_CLIENT=6
# Kill idle sessions with no active socket connections after this many ms.
TERMINAL_IDLE_TIMEOUT_MS=1800000
# Rolling output buffer kept per session (bytes).
TERMINAL_MAX_BUFFER_BYTES=2097152

# --------------------------------------------
# OpenClaw Gateway (for COZ agent integration)
# --------------------------------------------
# OPENCLAW_GATEWAY_HOST=localhost
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_GATEWAY_PASSWORD=your_gateway_password
# OPENCLAW_GATEWAY_TOKEN=your_gateway_token

# --------------------------------------------
# Storage Paths (Optional - defaults to relative paths)
# --------------------------------------------
# BRAIN_STORAGE_PATH=./storage/brains
# RUNS_PATH=./storage/runs

# --------------------------------------------
# Database URL (Managed by Prisma)
# --------------------------------------------
# Default: ./prisma/studio.db (relative to project)
# For global config, this is automatically set to ~/.evobrew/database.db
DATABASE_URL="file:./prisma/studio.db"

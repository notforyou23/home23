import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import test from 'node:test';

const HOME23_ROOT = process.cwd();

test('live-problems panel exposes an operator readout, not only raw verifier rows', () => {
  const js = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.js'), 'utf8');
  const css = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.css'), 'utf8');
  const html = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.html'), 'utf8');

  assert.match(html, /h23-problems-overlay-panel/);
  assert.doesNotMatch(html, /Seeded problems/);
  assert.doesNotMatch(html, /seedOrigin: system/);
  assert.doesNotMatch(html, />\+ Add problem</);
  assert.match(js, /renderProblemsOperatorSummary/);
  assert.match(js, /Operator Status/);
  assert.match(js, /Needed From You/);
  assert.match(js, /const activeProblems = problems\.filter/);
  assert.match(js, /const resolvedProblems = problems\.filter/);
  assert.match(js, /renderProblemHistoryDrawer\(resolvedProblems\)/);
  assert.match(js, /activeProblems\.map\(p => renderProblemCard\(p\)\)/);
  assert.doesNotMatch(js, /problems\.map\(p => renderProblemCard\(p\)\)/);
  assert.match(css, /\.h23-problems-history/);
  assert.match(js, /renderProblemEvidenceDrawer\(p, \{ last, lastChecked, stepsLabel, recentRem \}\)/);
  assert.match(js, /<summary>Verifier evidence<\/summary>/);
  assert.doesNotMatch(js, />edit<\/button>/);
  assert.doesNotMatch(js, /<div style="font-size:12px;color:rgba\(255,255,255,0\.6\);margin-bottom:6px;">\s*<span>verifier:/);
  assert.doesNotMatch(js, /recent attempts: \$\{recentRem\.map/);
  assert.match(css, /\.h23-problem-evidence/);
  assert.match(js, /problemRepairText/);
  assert.match(js, /renderProblemUserAction/);
  assert.match(js, /Action Needed/);
  assert.match(js, /Inspect Plan/);
  assert.match(js, /Mark Handled \+ Re-check/);
  assert.match(js, /recordProblemUserIntervention/);
  assert.match(js, /recordGoodLifeUserIntervention/);
  assert.match(js, /user-intervention/);
  assert.match(js, /Verifier remains the source of truth/);
  assert.match(js, /Re-check/);
  assert.match(js, /manual review needed; autonomous remediation has escalated this issue/);
  assert.match(js, /problem\.escalated === true/);
  assert.match(js, /nothing; this issue is resolved/);
  assert.match(js, /brief\.status === 'Paused'/);
  assert.match(js, /paused by budget/);
  assert.match(js, /brief\.status === 'Resting'/);
  assert.match(js, /resting autonomously/);
  assert.match(js, /goodLifePausedFleetText/);
  assert.match(js, /budget \$\{reset\}/);
  assert.match(js, /renderGoodLifeHostPressure/);
  assert.match(js, /renderGoodLifePm2Changes/);
  assert.match(js, /Runtime Changes/);
  assert.match(js, /renderGoodLifeWorkerReceiptDetail/);
  assert.match(js, /renderGoodLifeIssueWorkerReceipt/);
  assert.match(js, /Latest Worker Receipt/);
  assert.match(js, /renderGoodLifeIssueInterventionConsole/);
  assert.match(js, /Operator Intervention/);
  assert.match(js, /Recommended next step/);
  assert.match(js, /Projection Provenance/);
  assert.match(js, /provenance\.projection\?\.authority/);
  assert.match(js, /From The Inside arc/);
  assert.match(js, /provenance\.curriculumArc\.issuesRead/);
  assert.match(js, /provenance\.conflicts/);
  assert.match(js, /Ask \$\{escapeHtml\(workerName\)\} Worker/);
  assert.match(js, /openGoodLifeWorkers/);
  assert.match(js, /Run Engine Check/);
  assert.match(js, /h23-goodlife-primary-action/);
  assert.match(js, /Next Action/);
  assert.match(js, /Work Manifest/);
  assert.match(js, /manifest\.allowedTransition/);
  assert.match(js, /manifest\.sourceSurface/);
  assert.match(js, /Correction Tombstones/);
  assert.match(js, /correctionTombstones/);
  assert.match(js, /do_not_inherit_old_projection/);
  assert.match(js, /Restraint Receipts/);
  assert.match(js, /restraintReceipts/);
  assert.match(js, /Scheduler Sovereignty/);
  assert.match(js, /renderGoodLifeScheduler/);
  assert.match(js, /Doctrine Adoption/);
  assert.match(js, /doctrineAdoption\.reusable/);
  assert.match(js, /doctrineAdoption\.blocked/);
  assert.match(js, /renderGoodLifeRings/);
  assert.match(js, /Good Life three-ring status/);
  assert.match(js, /operatorRings/);
  assert.match(js, /formatGoodLifeWorkerResult/);
  assert.match(js, /Worker check complete: \$\{formatGoodLifeWorkerResult/);
  assert.match(js, /last change/);
  assert.match(js, /currently not online/);
  assert.match(js, /repair signal/);
  assert.match(js, /compactGoodLifeHostDetail/);
  assert.match(js, /Host Pressure/);
  assert.match(js, /CPU Load/);
  assert.match(js, /Top Process/);
  assert.match(js, /goodLifeHostPressureStatus/);
  assert.match(js, /drives rest/);
  assert.match(js, /informational/);
  assert.match(css, /\.h23-problems-operator/);
  assert.match(css, /\.h23-problem-operator-grid/);
  assert.match(css, /\.h23-problem-user-action/);
  assert.match(css, /\.h23-goodlife-fleet-row\.paused/);
  assert.match(css, /\.h23-goodlife-fleet-row\.resting/);
  assert.match(css, /\.h23-goodlife-evidence-row\.watch/);
  assert.match(css, /\.h23-goodlife-evidence-row\.info/);
  assert.match(css, /\.h23-goodlife-intervention-console/);
  assert.match(css, /\.h23-goodlife-intervention-grid/);
  assert.match(css, /\.h23-goodlife-primary-action/);
  assert.match(css, /\.h23-goodlife-primary-action \.h23-goodlife-mini-actions[\s\S]*flex:\s*0 0 auto/);
  assert.match(css, /\.h23-goodlife-rings/);
  assert.match(css, /\.h23-goodlife-ring\.needs-user/);
  assert.match(css, /\.h23-goodlife-overlay-body[\s\S]*grid-template-rows:\s*minmax\(180px,\s*0\.82fr\)\s*auto\s*minmax\(260px,\s*1\.18fr\)\s*auto/);
  assert.match(css, /\.h23-goodlife-overlay-top[\s\S]*overflow-y:\s*auto/);
  assert.match(css, /\.h23-goodlife-overlay-workspace[\s\S]*overflow:\s*hidden/);
  assert.match(css, /\.h23-goodlife-overlay-list,\s*\n\.h23-goodlife-overlay-detail[\s\S]*overflow-y:\s*auto/);
  assert.match(css, /\.h23-goodlife-overlay-action-status[\s\S]*pointer-events:\s*none/);
});

test('Good Life issue detail shows user-facing repair context before raw JSON', () => {
  const js = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.js'), 'utf8');
  const css = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.css'), 'utf8');

  assert.match(js, /h23-goodlife-issue-brief/);
  assert.match(js, /What is wrong/);
  assert.match(js, /What is happening/);
  assert.match(js, /Needed from jtr/);
  assert.match(js, /Stop condition/);
  assert.match(css, /\.h23-goodlife-issue-brief/);
});

test('Agency inspector exposes cron retirement proposals as a filtered proof-chain surface', () => {
  const js = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.js'), 'utf8');
  const css = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.css'), 'utf8');
  const html = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.html'), 'utf8');
  const server = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/server.js'), 'utf8');

  assert.match(html, /agency-retirement-proposals/);
  assert.match(html, /Cron Retirement Proposals/);
  const agencyStatsTag = html.match(/<div class="h23-worker-stats" id="agency-stats"[^>]*>/)?.[0] || '';
  const agencyBriefSectionTag = html.match(/<section class="h23-workers-section" id="agency-brief-section"[^>]*>/)?.[0] || '';
  const agencyStats = html.match(/<div class="h23-worker-stats" id="agency-stats"[^>]*>[\s\S]*?<\/div>\s*<\/div>/)?.[0] || '';
  assert.match(agencyStatsTag, /\bhidden\b/);
  assert.match(agencyBriefSectionTag, /\bhidden\b/);
  assert.doesNotMatch(agencyStats, /<div class="h23-worker-stat">/);
  assert.doesNotMatch(agencyStats, />—</);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-scratch-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-truth-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-organs-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-retirement-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-pursuits-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-receipts-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-inbox-drawer" hidden>/);
  assert.match(html, /<details class="h23-agency-evidence-drawer" id="agency-consequences-drawer" hidden>/);
  assert.doesNotMatch(html, /Private scratch and bootcamp editor receipts/);
  assert.doesNotMatch(html, /Source order, contradictions, and doctrine gates/);
  assert.doesNotMatch(html, /What each organ can sense, change, and must not do alone/);
  assert.doesNotMatch(html, /Why the resident kernel selected, discarded, or merged inputs/);
  assert.doesNotMatch(html, /Recent world-stream triage and discard decisions/);
  assert.doesNotMatch(html, /Observed changes and closure evidence/);
  assert.doesNotMatch(html, /<details class="h23-agency-evidence-drawer" id="agency-brief-drawer">/);
  assert.doesNotMatch(html, /<div class="h23-workers-section-title">Active Pursuits<\/div>/);
  assert.match(js, /\/home23\/api\/agency\/inspector\?filter=cron_retirement_proposals/);
  assert.match(js, /renderAgencyRetirementProposalRow/);
  assert.match(js, /revealAgencyInspectorContract/);
  assert.match(js, /revealAgencyEvidenceDrawers/);
  assert.match(js, /agency-retirement-drawer/);
  assert.match(js, /retirementDrawer\.hidden = !proposals\.length/);
  assert.match(js, /agencyOperatorNeedCount\(state, brief\)/);
  assert.match(js, /residentAgencyModeLabel\(state\.mode\)/);
  assert.match(js, /residentActionAuthorityLabel\(state\.nextAction\)/);
  assert.match(js, /<label>Needs jtr<\/label>/);
  assert.match(js, /<label>Next Authority<\/label>/);
  assert.doesNotMatch(js, /<label>Inbox<\/label>/);
  assert.doesNotMatch(js, /<label>Receipts<\/label>/);
  assert.doesNotMatch(js, /\$\{escapeHtml\(state\.mode \|\| 'unknown'\)\}/);
  assert.doesNotMatch(js, /<label>Recent Receipts<\/label>/);
  assert.doesNotMatch(js, /state\.attention\?\.queueDepth/);
  assert.match(js, /residentAgencyVisiblePursuits\(state, pursuits\)/);
  assert.match(js, /function residentAgencyVisiblePursuits\(state, pursuits = \[\]\)/);
  assert.match(js, /Array\.isArray\(state\.activePursuits\) \? state\.activePursuits : pursuits/);
  assert.match(js, /status !== 'discarded' && status !== 'closed'/);
  assert.doesNotMatch(js, /pursuits\.length \? pursuits\.map\(renderAgencyPursuitRow\)/);
  assert.match(js, /renderAgencyBriefQuestionBlock/);
  assert.match(js, /agencyMeaningfulBriefChanges/);
  assert.match(js, /summary !== 'pursuit_has_no_editor_block'/);
  assert.match(js, /kind !== 'explicit_no_change'/);
  assert.match(js, /Neighbor report from/);
  assert.match(js, /renderAgencyCandidateTitle/);
  assert.match(js, /Machine memory telemetry/);
  assert.match(js, /Machine CPU telemetry/);
  assert.match(js, /Work heartbeat/);
  assert.doesNotMatch(js, /<div>\$\{escapeHtml\(c\.title \|\| c\.summary \|\| c\.candidateId\)\}<\/div>/);
  assert.doesNotMatch(js, /<pre style="white-space:pre-wrap/);
  assert.doesNotMatch(js, /brief\.text/);
  assert.match(css, /\.h23-agency-brief-block/);
  assert.match(css, /\.h23-agency-brief-row/);
  assert.match(css, /\.h23-agency-evidence-drawer/);
  assert.match(css, /\.h23-agency-evidence-drawer > summary/);
  assert.match(js, /cron_run_log_excerpt/);
  assert.match(js, /semanticStatus/);
  assert.match(server, /\/home23\/api\/agency\/inspector/);
});

test('Home dashboard is the human front door, with agency detail behind the agency tab', () => {
  const js = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.js'), 'utf8');
  const css = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.css'), 'utf8');
  const html = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.html'), 'utf8');
  const server = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/server.js'), 'utf8');

  assert.match(html, /id="human-home"/);
  assert.match(html, /id="tz1-time"/);
  assert.match(html, /id="home-vibe-image"/);
  assert.match(html, /id="human-weather-value"/);
  assert.match(html, /id="human-sauna-value"/);
  assert.match(html, /id="human-sauna-gauge"/);
  assert.match(html, /id="human-sauna-target"/);
  assert.match(html, /id="human-sauna-duration"/);
  assert.match(html, /id="human-sauna-actions"/);
  assert.match(html, /id="human-pool-value"/);
  assert.match(html, /id="human-issues-value"/);
  assert.match(html, /id="human-goodlife-value"/);
  assert.match(html, /id="human-jerry-remark"/);
  assert.match(html, /id="human-jerry-context"/);
  assert.match(html, /data-home-tile-id="vibe"/);
  assert.match(html, /data-home-tile-id="outside-weather"/);
  assert.match(html, /data-home-tile-id="sauna-control"/);
  assert.match(html, /data-home-tile-id="pool-screenlogic"/);
  assert.match(html, /data-home-tile-id="good-life"/);
  assert.match(html, /data-home-tile-id="system-summary"/);
  assert.match(html, /href="\/home23\/chat"/);
  assert.doesNotMatch(html, /id="resident-home"/);

  assert.match(js, /setupHumanHomeSurface/);
  assert.match(js, /loadHumanHomeSurface/);
  assert.match(js, /refreshHomeTileLayout/);
  assert.match(js, /applyHomeTileLayout/);
  assert.match(js, /mutateHomeTileLayout/);
  assert.match(js, /\/home23\/api\/tiles\/config/);
  assert.match(js, /\/home23\/api\/settings\/tiles/);
  assert.match(js, /scheduleHumanHomeFetch/);
  assert.doesNotMatch(js, /const \[weather, sauna, pool, problems, goodLife, state, agency/);
  assert.match(js, /void loadVibeTile\(primaryAgent/);
  assert.match(js, /openVibeImageDetail/);
  assert.match(js, /home-vibe-detail-gallery/);
  assert.match(js, /\/home23\/api\/vibe\/gallery\/items\/\$\{encodeURIComponent\(item\.id\)\}/);
  assert.match(js, /\/home23\/api\/tiles\/outside-weather\/data/);
  assert.match(js, /\/home23\/api\/tiles\/sauna-control\/data/);
  assert.match(js, /\/home23\/api\/tiles\/pool-screenlogic\/data/);
  assert.match(js, /offlineTilePayload\('pool-screenlogic', 'Offline', '—', 'ScreenLogic unavailable'\)/);
  assert.match(js, /function offlineTilePayload/);
  assert.match(js, /\/home23\/api\/tiles\/sauna-control\/actions\/\$\{encodeURIComponent\(actionId\)\}/);
  assert.match(js, /renderHumanSauna/);
  assert.match(js, /setSaunaPreset/);
  assert.match(js, /runHumanSaunaAction/);
  assert.match(js, /renderJerryVoiceTile/);
  assert.match(js, /\/api\/pulse\/latest/);
  assert.match(js, /\/api\/home\/summary/);
  assert.match(js, /currentTab === 'home'\) loadHumanHomeSurface/);
  assert.ok(js.includes("if (currentTab === 'agency') {\n        loadAgencySurface().catch(renderAgencySurfaceError);"));

  assert.match(css, /\.h23-human-home/);
  assert.match(css, /\.h23-human-card-vibe/);
  assert.match(css, /\.h23-human-card-vibe[\s\S]*aspect-ratio/);
  assert.match(css, /\.h23-human-vibe-image[\s\S]*aspect-ratio/);
  assert.match(css, /\.h23-vibe-detail-modal/);
  assert.match(css, /\.h23-vibe-detail-image/);
  assert.match(css, /\.h23-human-sauna-gauge/);
  assert.match(css, /\.h23-human-sauna-control/);
  assert.match(css, /\.h23-human-jerry/);
  assert.match(css, /\.h23-human-metrics/);
  assert.match(css, /\.h23-home-tile-tools/);
  assert.match(css, /\[data-home-tile-size="half"\]/);
  assert.match(css, /\[data-home-tile-size="full"\]/);
  assert.match(server, /resident agency state/);
});

test('Dashboard exposes Jerry and Forrest briefs as readable human documents', () => {
  const js = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.js'), 'utf8');
  const css = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.css'), 'utf8');
  const html = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.html'), 'utf8');
  const server = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/server.js'), 'utf8');

  assert.match(html, /data-tab="briefs"/);
  assert.match(html, /id="human-briefs-list"/);
  assert.match(html, /id="panel-briefs"/);
  assert.match(html, /id="briefs-reader"/);
  assert.match(js, /loadBriefsSurface/);
  assert.match(js, /pendingSelectedId/);
  assert.match(js, /showBriefsReader/);
  assert.match(js, /showBriefsList/);
  assert.match(js, /data-briefs-back/);
  assert.match(js, /renderHomeBriefs/);
  assert.match(js, /\/home23\/api\/briefs\?limit=/);
  assert.match(js, /\/home23\/api\/briefs\?limit=12&compact=1/);
  assert.match(js, /function isHomeBriefItem/);
  assert.match(js, /item\.status === 'error'/);
  assert.match(js, /item\.type === 'session'/);
  assert.match(js, /briefsState/);
  assert.match(css, /\.h23-briefs-reader/);
  assert.match(css, /\.h23-briefs-reader-back/);
  assert.match(css, /#panel-briefs\.h23-briefs-reading \.h23-briefs-list/);
  assert.match(css, /\.h23-human-briefs/);
  assert.match(server, /Home23BriefsService/);
  assert.match(server, /\/home23\/api\/briefs/);
});

test('Home dashboard exposes a mounted native chat tile, not only a standalone link', () => {
  const html = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.html'), 'utf8');
  const css = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-dashboard.css'), 'utf8');
  const chatCss = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-chat.css'), 'utf8');
  const chatJs = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-chat.js'), 'utf8');

  assert.match(html, /data-home-tile-id="chat"/);
  assert.match(html, /id="chat-slot-tile"/);
  assert.match(html, /id="chat-agent-pill"/);
  assert.match(html, /id="chat-model-pill"/);
  assert.match(html, /id="chat-more-btn"/);
  assert.match(html, /id="chat-expand-btn"/);
  assert.match(html, /data-action="open-standalone"[\s\S]*Open in new tab/);
  assert.match(html, /id="chat-conv-panel"/);
  assert.match(html, /id="chat-conv-back-btn"/);
  assert.match(html, /id="chat-conv-new-btn"/);
  assert.match(html, /id="chat-conv-list"/);
  assert.match(css, /\.h23-human-card-chat/);
  assert.match(css, /\.h23-human-card-chat[\s\S]*overflow:\s*hidden/);
  assert.match(css, /\.h23-human-card-chat \.h23-chat-slot[\s\S]*flex:\s*1 1 0/);
  assert.match(css, /\.h23-human-card-chat \.h23-chat-conv-panel[\s\S]*overflow:\s*hidden/);
  assert.match(chatCss, /\.h23-chat-conv-head/);
  assert.match(chatJs, /maybeAutoInitDashboardChat/);
  assert.match(chatJs, /chatMode = mode \|\| 'tile'/);
  assert.match(chatJs, /const shouldResumeHistory = chatMode === 'standalone'/);
  assert.match(chatJs, /preferLatest: shouldResumeHistory/);
  assert.match(chatJs, /setupConversationPanelControls/);
  assert.match(chatJs, /closeConversationList/);
  assert.match(chatJs, /dashboard tile is the human/);
});

test('Chat history route uses bounded reads and keeps machine ledgers out of chat resume', () => {
  const server = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/server.js'), 'utf8');
  const chatJs = fs.readFileSync(path.join(HOME23_ROOT, 'engine/src/dashboard/home23-chat.js'), 'utf8');

  assert.match(server, /readJsonlTailLines/);
  assert.match(server, /parseConversationFile = \(filePath, limit\)/);
  assert.doesNotMatch(server, /readFileSync\(filePath, 'utf8'\)\.trim\(\)/);
  assert.match(server, /const listLimit = Math\.max/);
  assert.match(server, /\.sort\(\(a, b\) => b\.stat\.mtimeMs - a\.stat\.mtimeMs\)/);
  assert.match(server, /\.slice\(0, listLimit\)/);
  assert.match(server, /isMachineConversation/);
  assert.match(server, /cron-decisions/);
  assert.match(server, /diagnose_/);
  assert.match(chatJs, /isResumableConversation/);
  assert.match(chatJs, /chatConversations\.find\(isResumableConversation\)/);
});

# Receipt — walk-away rails (Good Life + notify fuse-box)

Date: 2026-07-17

## Changes

1. **Good Life** (`engine/src/good-life/objective.js`)
   - `schedulerFailingJobsCritical`: 1 → **3**
   - 1–2 failing jobs → **friction scenery**, not viability `repair`

2. **notify_jtr** (`engine/src/live-problems/loop.js` + `seed.js`)
   - Loop starves `notify_jtr` unless `args.fuseBox: true` or severity `critical`/`emergency`
   - Fuse-box seeds: disk free, create_file tool, harness online, dashboard ping/port, engine admin, brain graph empty
   - Cron/health/sensors/synthesis/CPU/etc. stay in plans but **do not page**

## Still needed to walk away

Commit the starve + walk-away package (item 1 from prior list) when you authorize it.

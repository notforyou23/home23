# Session 2026-07-02 Handoff - Home23 1.0 Release Prep

## Headline

Home23 was prepared for the 1.0.0 release line without deleting local runtime data or resetting the live checkout. The branch was already current with `origin/main`; the release work focused on validating the large pending Home23/COSMO/contract changes, fixing test-blocking bugs, and adding clean onboarding docs.

## Code Fixes

- `engine/src/agency/resident-kernel.js`
  - Live-problem verifier receipts and scheduler cron binding receipts now count as independent system evidence for closing resident pursuits.
  - Self-only closure evidence still becomes `provisional`, preserving the witness-independence guard.
- `engine/src/publish/bridge-chat-publisher.js`
  - Stale high-salience action requests are still suppressed from chat, but now leave a `stale_signal_deferred` log receipt for operator review.
- `engine/src/dashboard/server.js`
  - Cleaned trailing whitespace found by `git diff --check`.

## Release/Onboarding Updates

- `package.json` and `package-lock.json` now report `1.0.0`.
- `README.md` now points to the 1.0 release, links `docs/ONBOARDING.md`, and includes the release validation command set.
- `CHANGELOG.md` has a `1.0.0 (2026-07-02)` entry.
- `docs/ONBOARDING.md` gives a short install/start/validate/runbook path for the next operator.

## Verification

Passed:

```bash
npm run build
npm test
npm run test:contracts
node --test --test-concurrency=1 tests/cosmo23/pgs-engine.test.cjs tests/cosmo23/query-engine-context.test.cjs tests/cosmo23/query-engine-runtime.test.cjs tests/cosmo23/anthropic-client-request.test.cjs tests/cosmo23/anthropic-provider-adapter.test.cjs tests/cosmo23/opus-model-catalog.test.cjs cosmo23/server/lib/run-artifact-inventory.test.js
npm run test:contracts:live
git diff --check
```

Key receipts:

- `npm test`: 582 tests passed.
- `npm run test:contracts`: 12 passed, 1 skipped fixture/live placeholder.
- Focused COSMO23 regressions: 45 tests passed.
- `npm run test:contracts:live`: read-only live routes checked for agent roster, client capabilities, chat models/history, settings, query catalog, home surfaces, sauna tile, and worker agents. Action probes remained skipped unless `HOME23_LIVE_CONTRACTS_ACTIONS=1` is set.

## Live State During Release Prep

Verified on 2026-07-02:

- Home23-family PM2 processes were online.
- Jerry `/api/state` responded with cycle `37807` and about `126313` memory nodes at the first check.
- COSMO23 `/api/status` was healthy idle: `running=false`, `lifecycle=idle`, `activeRun=false`, `blockedReason=null`.
- Good Life was in `repair` mode because the live checkout has one chronic scheduler problem and host pressure, not because the package tests failed.

Live caveat intentionally not changed:

- `jerry_harness_cron_jobs_healthy` remained `chronic`.
- The verifier reported 8 enabled cron jobs with repeated errors, mostly `empire-*` and `imac-*` jobs.
- Remediation was already exhausted and the live problem was escalated. No cron jobs were disabled, deleted, or modified during release prep because that is live operator state, not a fresh-install package blocker.

## Next Operator Notes

- For a fresh install, use `docs/ONBOARDING.md`.
- For release verification, run the commands in the README or this handoff.
- For jtr's live checkout, resolve the chronic scheduler problem as a separate operator decision before claiming Good Life is fully green.
- Do not use broad PM2 or git destructive commands; preserve `instances/` runtime data and local schedules.

export interface RestoreRehearsalAdapter{snapshotHash(path:string):Promise<string>;restore(input:{backupPath:string;destinationPath:string;expectedSha256:string}):Promise<{destinationSha256:string}>;}
export async function rehearsePrecompactRestore(adapter:RestoreRehearsalAdapter,input:{backupPath:string;restorePath:string;precompactHash:string}){const restored=await adapter.restore({backupPath:input.backupPath,destinationPath:input.restorePath,expectedSha256:input.precompactHash});const actual=await adapter.snapshotHash(input.restorePath);if(restored.destinationSha256!==input.precompactHash||actual!==input.precompactHash)throw new Error("restore rehearsal hash mismatch");return Object.freeze({precompactHash:input.precompactHash,restoredHash:actual,equal:true as const});}
